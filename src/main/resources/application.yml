spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/accounting_department
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
  thymeleaf:
    mode: HTML
    cache: false

server:
  port: 8088

security:
  basic:
    enabled: false
